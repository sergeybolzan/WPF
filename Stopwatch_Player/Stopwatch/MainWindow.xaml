<Window x:Class="Stopwatch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="СЕКУНДОМЕР" Height="350" Width="380" MinHeight="350" MinWidth="380" WindowStartupLocation="CenterScreen">
    <DockPanel LastChildFill="True">
        <DockPanel.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="LightBlue" Offset="0"/>
                <GradientStop Color="MidnightBlue" Offset="1"/>
            </LinearGradientBrush>
        </DockPanel.Background>
        <TextBox 
            x:Name="tbTimer"
            DockPanel.Dock="Top" 
            HorizontalAlignment="Center" 
            Margin="30" FontSize="30" 
            FontWeight="SemiBold" 
            Foreground="Yellow" 
            Background="Black" 
            Text="00:00:00:000"
            IsReadOnly="True">
        </TextBox>
        <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Margin="10" Width="100" FontSize="20" FontWeight="Bold" Click="Button_Click">ЗАПУСК</Button>
            <Button Margin="10" Width="100" FontSize="20" FontWeight="Bold" Click="Button_Click_1">СБРОС</Button>
            <Button Margin="10" Width="100" FontSize="20" FontWeight="Bold" Click="Button_Click_2">КРУГ</Button>
        </StackPanel>
        <ListBox x:Name="lbMarks" Margin="15" FontSize="20" FontWeight="SemiBold">
            <ListBox.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FFF5DEDE" Offset="0"/>
                    <GradientStop Color="#FFAAA2A2" Offset="1"/>
                </LinearGradientBrush>
            </ListBox.Background>
        </ListBox>
    </DockPanel>
</Window>
